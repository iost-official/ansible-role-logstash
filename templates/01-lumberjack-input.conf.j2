input {
  lumberjack {
  	host => "{{ logstash_listen_host }}"
    port => {{ logstash_conf_port }}
    type => "lumberjack"
    congestion_threshold => {{ logstash_threshold }}
    enable_metric => true
    ssl_certificate => "{{ logstash_ssl_dir }}/{{ logstash_ssl_certificate_file | basename }}"
    ssl_key => "{{ logstash_ssl_dir }}/{{ logstash_ssl_key_file | basename }}"
    ssl_verify_mode => "force_peer"
{% endif %}
  }
}
